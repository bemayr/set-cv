import { FunctionalComponent, h } from "preact";
import style from './style.css';

const Camera: FunctionalComponent = () => {
  return (
    <div class={style.camera}>
    </div>
  );
};

export default Camera;
